---
layout: post
title: "/jekyll/README.md"
date: 2020-01-21 03:15:00
tags: build
---

This location is intended to be the top-level directory of a Jekyll project.

Merge jekyll into this location with `jekyll new . --force`

Change the site settings in `_config.yml` to tune to your environment.

Finally build and export with `jekyll build -d <destdir>`
